services:
  api:
    build:
      context: .
      dockerfile: docker/api.debug.Dockerfile

    ports:
      - ${API_PORT}:8080
      - "40000:40000"

    volumes:
      - .:/app

    env_file:
      - .env


    